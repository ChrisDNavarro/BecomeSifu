<UserControl x:Class="BecomeSifu.Pages.PracticeGif"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:BecomeSifu.Pages"
             xmlns:gif="http://wpfanimatedgif.codeplex.com" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ItemsControl x:Name="PracticeGifIC">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Image gif:ImageBehavior.AnimatedSource="{Binding SourceIgnorable}" gif:ImageBehavior.RepeatBehavior="{Binding Practiced}" 
                           gif:ImageBehavior.AnimationCompleted="UpdatePracticing" Stretch="UniformToFill" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="-75,0,0,0" >
                        <Image.Effect>
                             <BlurEffect Radius="50"/>
                        </Image.Effect>
                    </Image>
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>

</UserControl>
